<%= link_to "Create a new trip", new_trip_path %> |
<% if @trip %>
  <%= link_to "Modify trip", edit_trip_path(@trip) %> |
  <%= link_to "New Objective", new_objective_path %>
<% end %>
<% if @objective %>
  
<%= button_to "Delete objective", { action: "destroy", id: @objective.id }, method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>
