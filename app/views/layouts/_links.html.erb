
<%= link_to "Create a new trip", new_trip_path %> | <%= link_to "Add Location", new_location_path %> |

<% if @trip %>
  <%= link_to "Modify trip", edit_trip_path(@trip) %> |
  <%= link_to "Add Objective", new_trip_objective_path(@trip) %>
<% end %>

<% if @objective %>
  <% if @objective.id != nil%>
| <%= link_to "Modify Trip Objective", edit_trip_objective_path(@trip, @objective) %>
  <% end %>
<% end %>
