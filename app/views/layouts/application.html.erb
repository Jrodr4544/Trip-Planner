<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>Trip-Planning Made Easy</title>
    <!-- <%#= include_gon %> -->

    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">

    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <link href="/assets/css/ie10-viewport-bug-workaround.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="/jumbotron-narrow/jumbotron-narrow.css" rel="stylesheet">
  
    <!-- Just for debugging purposes. Don't actually copy these 2 lines! -->
    <!--[if lt IE 9]><script src="../../assets/js/ie8-responsive-file-warning.js"></script><![endif]-->
    <script src="/assets/js/ie-emulation-modes-warning.js"></script>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
  <body>
    <div class="container">
      <div class="header clearfix">
        <nav>
          <ul class="nav nav-pills pull-right">
          <div style="max-width:500px;"><%= link_to image_tag("my_logo.png", border:0), trips_path, class: "img-responsive" %></div>
            <li role="presentation" class="<%= controller.class == WelcomeController and controller.action_name == 'home' ? 'active' : '' %>"><%= link_to "Home", root_path %></li>

            <% if user_signed_in? %>
            <%= javascript_include_tag :defaults %>
              <li role="presentation" ><%= link_to('Logout', (destroy_user_session_path)) %></li>
              <li role="presentation" class="<%= controller.class == UsersController and controller.action_name == 'show' ? 'active' : '' %>"><%= link_to (current_user.name+"'s trips"), user_path(current_user) %></li>
            <% else %>
              <li role="presentation" class="<%= controller.class == Devise::SessionsController and controller.action_name == 'new' ? 'active' : '' %>"><%= link_to "Sign in", new_user_session_path %></li>
            <% end %>
            <li role="presentation"><a href="#">About</a></li>
            <li role="presentation" class="<%= controller.class == LocationsController and controller.action_name == 'index' ? 'active' : '' %>"><a href="/locations">Locations</a></li>
          </ul>
        </nav>
        

      </div>
    <%= yield %>
      

      

      <footer class="footer">
        <p>&copy; 2016 Trip-Planner by: Jelson Rodriguez</p>
      </footer>

    </div> <!-- /container -->


    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <script src="/assets/js/ie10-viewport-bug-workaround.js"></script>
  </body>
</html>
